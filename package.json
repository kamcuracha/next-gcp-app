{
  "name": "next-gcp-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p 8080",
    "gcp-build": "npm run build",
    "deploy": "gcloud config set project vaulted-codex-269423 && gcloud app deploy app.yaml --version=0-1-0"
  },
  "dependencies": {
    "next": "^9.5.5",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  }
}
